(*

fun is_older() =

val test1 = is_older((1,2,3), (2,3,4)) = true

val d1 = (2017, 9, 28)
val d2 = (2017, 12, 25)

val test1 = (is_older(1,2,3),(2,3,4) = true)
val test2 = (is_older

*)

fun get_nth(strings, n) =
    if n = 1
    then (hd strings)
    else( get_nth(t1 strings), n-1)

val test1 = (get_nth(=

