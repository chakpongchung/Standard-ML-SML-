fun is_older((y1,m1,d1), (y2,m2,d2)) =
    if y1 = y2
    then if m1 = m2
    	 then d

fun oldest(dates) =
    case dated of
    [] => NONE
    | date :: [] => SOME date
    | date :: rest []let
			val oldest_in_tail = oldest(rest)
			in
				if is_older(date,valOf, oldest_ in tail)
				then SOME data
			n

